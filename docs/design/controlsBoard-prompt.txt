Mode: built-in image_gen
Use case: ui-mockup. Asset type: implementation specification board for the ALREADY SELECTED ianvs_design Flutter Material 3 + macOS compact desktop system. This is not a new direction. Ground every widget in the attached selected dark design: graphite surfaces, 6px radii, system sans/PingFang SC, small labels13, blue selection/focus, real native Material interaction shapes. Create one legible 1920x1440 component specimen board with 3 orderly columns on the same dark surface, titled 'ianvs design / 操作、输入与选择'. This is intentionally a component reference, not a marketing screen.
Column1 '操作': Filled button '保存', tonal '添加', outlined '取消', text '了解更多', elevated '新建'; show disabled and loading below, focus-ring example; icon buttons outline/filled; small standard FAB plus extended FAB; segmented single selection '通用 Terminal ACP'.
Column2 '输入': default field '名称' with 'Local Shell', blue focus field, empty required error field '请输入启动命令', disabled field, password field, multiline field, compact search with icon, autocomplete suggestion popup, dropdown select with native anchored menu.
Column3 '选择': checkbox unchecked/checked/mixed, radio unselected/selected, switch off/on, slider and range slider, ActionChip '运行', FilterChip selected '本地', ChoiceChip '紧凑', InputChip '标签' with remove icon.
Show compact actual controls consistently and readable small subcaptions. Focus/error/disabled should be distinguishable without only color. Grayscale neutral surfaces, blue emphasis, error readable coral. Do not invent new visual style, shadows except popup. No illustration, fake logo, gradients, or oversized pills apart from naturally pill-shaped switch/chips/FAB. Keep all specimens on the same flat base with dividers, no nested cards. One design board, typography big enough to inspect at1920x1440. Every component uses authentic Material building blocks with macOS density, not a simplistic drawing.
