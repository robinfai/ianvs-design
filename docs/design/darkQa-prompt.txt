Mode: built-in image_gen
Use case: compositing. Asset type: designer visual QA annotation board.
Compare the two attached Ianvs Design dark desktop screens at the same 1487x1058 viewport. Image 1 is the USER SELECTED reference; image 2 is an ACTUAL Flutter build screenshot. Create one high resolution side by side annotated comparison board, label each image accurately and preserve their content and geometry; do not redraw the app or silently improve the actual screenshot. Add a restrained bottom findings strip with numbered callouts and specific observed differences for layout, spacing, typography, input/action sizes, colors and icons. Chinese or short English annotations must be legible. Highlight meaningful fidelity problems rather than stylistic inventions. Treat actual pixel screenshots as evidence, the annotations as designer judgement.
Context: this is a reusable Material 3 library adopting macOS conventions; compact specification is label 13, controls min32, radius6; the selected reference contains these written tokens although its rendered controls may be visually larger. Extra sidebar Design Tokens entry is requested functional coverage, and the empty error field correctly matches required validation, so label those as intentional adaptations, not defects. Identify remaining visible differences with concrete recommended alignment/spacing fixes. No fake success stamp, no claims about keyboard behavior derived from images. Board title 'Ianvs Design · Dark visual review · Pass 1'.
