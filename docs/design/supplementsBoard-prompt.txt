Mode: built-in image_gen
Use case: ui-mockup. Create a production design specification board for three supplementary Flutter components in the existing Ianvs Design system. This is an extension of the selected design, not a new visual direction.
Images 1 and 2 are Ianvs dark and light visual truth. Image 3 is the official TDesign number stepper reference, image 4 the official cascader pattern; borrow interaction ideas only, all surfaces, typography, spacing, color and icons must be restyled to Ianvs.
Board: two equal columns DARK and LIGHT, same three sections in both; label 'Ianvs Design · Extended controls'. Flat professional screenshot quality. Desktop system font, labels13, titles16, controls32px compact with 6px radius, 8/12/16/24 spacing. Dark graphite surfaces #202123, fields#292b2e borders#45474d, blue#0874df. Light #fafafb/white and #0969da. Material outline icon consistency, no marketing art, no browser/mock-device frame.
1. Cascader: inline 440px panel, top breadcrumb buttons '全部 > 本地 > Shell', list choices '登录 Shell' selected with check, '交互 Shell', '受限 Shell' disabled. Clear branch chevron vs leaf check. Separate caption: '选择父级会清空下级；Tab / Enter 选择；路径可回溯'. One focus-ring row shown.
2. Number stepper: horizontal minus icon button, centered editable value13, plus button. Label '终端字号'; adjacent min8/max32 states and disabled example. Small caption '输入或方向键调整；Enter 提交；Esc 还原；边界禁用'.
3. Skeleton: subtle neutral text bars with title line and three body rows, last line shorter; no shimmer glare. Next to it actual loaded list text, clear transition from structure placeholder to contents. Caption '延迟显示，减少闪烁；尊重减少动画'. Layout reserves the same space.
Bottom thin token strip. Render all Chinese legibly. Do not copy TDesign visual branding, giant type or mobile sheet geometry into the desktop library.
