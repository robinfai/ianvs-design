Use case: ui-mockup
任务：对 Ianvs Design 导航抽屉本轮修改做独立视觉验收，输出中文横向验收板。输入是未缩放原始截图的前后并排对照；BEFORE/AFTER标签在产品外。重点是主内容区内的 NavigationDrawer，不是最左侧Gallery目录。F01旧版蓝色选中矩形在容器中间、图标文字在左侧。逐项检查修改后：背景是否覆盖对应图标和标签、三个目的地是否一致、桌面深浅与480px窄屏是否对齐、选中层级是否清楚。深色前后所选目的地可能不同，请按各自选中行对比布局，不把文字变化当成缺陷。第四张补充主页和设置状态。
输出：每项标「通过/不通过/证据不足」，引用原始编号，最多4条检查结论与2条残留建议。只根据截图，不宣称键盘、读屏或完整功能测试通过。不得重绘错误证据或将正常滚动截面当成裁切缺陷。标题「导航选中状态 · ImageGen 视觉验收」，注明「仅视觉验收」。保留现有紧凑Material桌面风格，不做风格重设计，不默认通过。
